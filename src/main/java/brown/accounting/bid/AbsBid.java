package brown.accounting.bid;

//AMY: where is the hashmap? it's not part of AbsBid, so how is AbsBid a hashmap?
/**
 * Abstract class for the lowest level bid datatype, which is a wrapped hashmap.
 * 
 * Motivation for this is to abstract the bid type to avoid having to write if/else
 * statements based on a bundletype enum. 
 * 
 * @author andrew
 */
public abstract class AbsBid {
  
}